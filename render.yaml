services:
  - type: web
    name: polestar-proxy
    runtime: node
    rootDir: proxy
    buildCommand: npm install
    startCommand: node index.js
    plan: free
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
